
define(['services/services'],
  function(services) {
    services.factory('processFactory', ['$http', function ($http) {
		var urlBase = 'http://10.188.192.200:8080';
		var processFactory = {};  
		var processUrl = '/process';

		
	}])
})